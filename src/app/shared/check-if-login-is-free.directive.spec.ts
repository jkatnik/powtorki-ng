import { CheckIfLoginIsFreeDirective } from './check-if-login-is-free.directive';

describe('CheckIfLoginIsFreeDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckIfLoginIsFreeDirective();
    expect(directive).toBeTruthy();
  });
});
